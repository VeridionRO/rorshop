- user = current_user
.main
  .clear.currency-bg
    #logo
      %a{href: "/"}
        %img{alt: "", src: "/assets/logo.jpg"}/
    .float-right
      .currency
        .moduletable
          - if is_logged_in
            %h3 
              Bine ai venit 
              = link_to user.name, user_path(user)
          - else
            %h3 Bine ai venit vizitatorule
      .cart
        .moduletable2
          - if @shopping_cart.total_unique_items != 0
            = @shopping_cart.total_unique_items
            produs(e) -
            = @shopping_cart.total
            RON
            %a{href: "#{shopping_cart_path(@shopping_cart)}"} Vizualizeaza
          - else
            Cosul dumneavoastra e gol